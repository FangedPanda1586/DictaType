DictaType RC1 - Exam room responsiveness + room details fix

Replace/add ONLY these files in the repository:

  dictatype/classroom.py
  dictatype/ui.py
  tests/test_classroom_async_room_details.py
  CHANGELOG.md

Keep your current public RC1 versions of:

  .github/workflows/windows-build.yml
  DictaType.spec
  installer.iss
  dictatype/tts.py

What this patch changes:
- Exam audio preparation runs in a background worker instead of blocking Tk.
- The room address and session code appear immediately.
- Students cannot join until exam audio is ready.
- Classroom & Exams is vertically scrollable on smaller screens.
- A Room Details banner at the top shows address, code, READY/preparing state and progress.

After uploading, commit all four files together and start a NEW GitHub Actions build.
