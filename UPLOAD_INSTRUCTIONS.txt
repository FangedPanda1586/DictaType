DictaType French neural runtime v7 (ONEDIR)
===========================================

Replace these files in the GitHub repository in ONE commit:

1. DictaType.spec
2. installer.iss
3. .github/workflows/windows-build.yml
4. dictatype/tts.py
5. tests/test_french_onedir_release.py

Then run a NEW workflow:
Actions -> Build Windows release - French v7 onedir -> Run workflow

Expected pipeline marker:
DictaType pipeline: french-v7-onedir-native-runtime

Important change:
PyInstaller now builds an internal one-folder runtime at dist/DictaType/. The
installer still gives users a normal DictaType installation and shortcut. The
portable ZIP contains the same tested runtime folder.
