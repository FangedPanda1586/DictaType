DictaType RC1 - Exam LAN open-join patch

Upload/replace these files in the repository:

  dictatype/classroom.py
  dictatype/ui.py
  tests/test_exam_network_open_join.py
  CHANGELOG.md

Keep the current public RC1 GitHub Actions workflow, DictaType.spec, installer.iss, and dictatype/tts.py.

Expected behaviour after rebuilding:
- Start an Exam on the teacher PC.
- DictaType listens on all local IPv4 network adapters.
- PCs on the same Wi-Fi/Ethernet LAN open the displayed http://<teacher-ip>:<port> address in a browser.
- Students enter their own name, class, and session code.
- New student profiles are allowed by default in Exam mode.
- Uncheck Open join only if you intentionally want a pre-registered roster.
- If Windows Firewall prompts on the teacher PC, allow DictaType on Private networks.
